function About() {
    return( // rendering the content in it //
        // we need our elements wrapped in parent containers, aka divs//
        <div> 
            <h1> Learn more about us </h1>
            <p> This is our first react app! </p>
        </div>
    );
}
export default About; // created a file that App.js can reference. //